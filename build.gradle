apply plugin: 'java'
apply plugin: 'maven-publish'

group 'com.github.InkApplications'
version '21.0'
sourceCompatibility = 1.6
targetCompatibility = 1.6

publishing {
  publications {
    mavenJava(MavenPublication) {
        artifactId 'guava-stopwatch'

        from components.java
    }
  }
}
